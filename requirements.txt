# =============================================================================
# Dépendances du projet : Pipeline de surveillance du risque boursier - Fortuneo
# =============================================================================
# Installer les dépendances avec : pip install -r requirements.txt

# --- Récupération des données boursières ---
yfinance>=0.2.50          # Bibliothèque de récupération des données boursières via Yahoo Finance

# --- Manipulation et analyse des données ---
pandas==2.2.1             # Manipulation des DataFrames et séries temporelles
numpy==1.26.4             # Calcul numérique vectorisé (indicateurs techniques, statistiques)

# --- Base de données et ORM ---
sqlalchemy==2.0.29        # ORM Python pour interagir avec la base de données de façon abstraite
# psycopg2-binary==2.9.9  # Décommenter pour une connexion PostgreSQL / AWS RDS en production

# --- Machine Learning ---
scikit-learn==1.4.1.post1 # Bibliothèque ML : Isolation Forest pour la détection d'anomalies
joblib==1.3.2             # Sérialisation et sauvegarde des modèles entraînés sur disque

# --- Suivi des expériences MLOps ---
mlflow==2.11.1            # Plateforme MLOps : traçabilité des expériences, versioning des modèles

# --- Stockage au format colonnaire ---
pyarrow==15.0.2           # Lecture et écriture de fichiers Parquet (format optimisé pour AWS S3)

# --- Visualisation ---
matplotlib==3.8.3         # Création de graphiques pour l'analyse exploratoire et les rapports
seaborn==0.13.2           # Visualisations statistiques avancées basées sur matplotlib

# --- Gestion de la configuration ---
pyyaml==6.0.1             # Lecture des fichiers de configuration au format YAML

# --- Tests unitaires ---
pytest==8.1.1             # Framework de tests unitaires et d'intégration
pytest-cov==5.0.0         # Mesure de la couverture de code lors des tests
